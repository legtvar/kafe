stages:
  - build
  - deploy

build-api:
  image: alpine:latest
  stage: build
  script:
    - apk update
    - apk add dotnet6-sdk
    - dotnet build Kafe.sln
  tags:
    - shared-fi
  cache:
    paths:
      - $HOME/.nuget
  only:
    - master
    - ci
    - api

build-web:
  image: alpine:latest
  stage: build
  script:
    - apk update
    - apk add npm
    - cd Web
    - npm ci
    - npm run build
  tags:
    - shared-fi
  cache:
    paths:
      - Web/node_modules
  artifacts:
    untracked: true
  only:
    - master
    - ci
    - web

pages:
  stage: deploy
  dependencies: 
    - build-web
  script:
    - cp Wep/build public
  tags:
    - shared-fi
  artifacts:
    paths:
      - public
  only:
    - master
    - ci
    - web