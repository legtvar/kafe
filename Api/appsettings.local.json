{
    "ConnectionStrings": {
        "postgres": "host=localhost;database=postgres;password=password;username=postgres",
        "KAFE": "host=localhost;database=kafe;password=password;username=postgres"
    }
}
